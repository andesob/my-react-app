import React, { Component } from 'react';

class Header extends Component{
    render() {
      return(
        <header>
            isthislive?
            maybenot but not sure
        </header>  
      );
    }
  }

  export default Header;